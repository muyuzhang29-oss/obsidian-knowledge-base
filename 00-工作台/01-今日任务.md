---
aliases: [今日任务, 今日待办]
tags: [工作, 任务]
created: 2026-05-13
---

# 📋 今日任务

## 🎯 工作任务

```dataview
TASK
FROM "08-Projects" OR "09-Issues"
WHERE !completed
SORT due ASC
LIMIT 10
```

## 📚 学习任务

```dataview
TASK
FROM "01-SV语法" OR "02-UVM" OR "03-Protocol"
WHERE !completed
SORT file.mtime DESC
LIMIT 5
```

## 🌱 生活任务

```dataview
TASK
FROM "12-Life"
WHERE !completed
SORT due ASC
LIMIT 5
```

---

## 📊 任务统计

| 分类 | 数量 |
|------|------|
| 🎯 工作任务 | `=length(filter(filter(link("").file.tasks, (x) => !x.completed), (x) => contains(x.path, "08-Projects") OR contains(x.path, "09-Issues")))` |
| 📚 学习任务 | `=length(filter(filter(link("").file.tasks, (x) => !x.completed), (x) => contains(x.path, "01-SV语法") OR contains(x.path, "02-UVM") OR contains(x.path, "03-Protocol")))` |
| 🌱 生活任务 | `=length(filter(filter(link("").file.tasks, (x) => !x.completed), (x) => contains(x.path, "12-Life")))` |

---

*最后更新: `=dateformat(date(now), "yyyy-MM-dd HH:mm")`*